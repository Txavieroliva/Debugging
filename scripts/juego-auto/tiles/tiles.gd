extends Node3D

#var speed = 20.0  # Exacto igual al jugador
#
#func _process(delta: float) -> void:
	#position.z -= speed * delta
	#if position.z < -200:  # Seguridad
		#print("Warning: Tile en Z bajo=", position.z, " – Posible gap si no borrado")
